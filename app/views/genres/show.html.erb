<h1><%=@genre.name%></h1>
<h2>Movies</h2>
<ul>
  <% @genre_movies.each do |m|%>
  <li><%= link_to m.title, movie_path(m.slug)%></li>
  <%end%>
</ul>
<%= link_to "<button>Go back</button>".html_safe, genres_path, :id => "button_id", :class => "button_class"%>
